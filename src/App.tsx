import React, { FC } from 'react'
import { Icon, IconTypes } from './components'

const App: FC = () => {
  return (
    <div>
      <Icon type={IconTypes.bell} />
    </div>
  )
}

export default App
